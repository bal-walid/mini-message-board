<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <ul>
    <% messages.forEach(message => { %>
        <li><%= message.user + ", " + formatDate(message.added) + ": " + message.text %></li>
    <% }) %>
  </ul>
</body>
</html>